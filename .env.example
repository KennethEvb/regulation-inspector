# GEMINI_API_KEY: Required for the /api/parse-regulation serverless function.
# Local dev: create a .env.local file (gitignored) with this value and run `vercel dev`.
# Production: set this in Vercel → Project Settings → Environment Variables (never commit the real key).
GEMINI_API_KEY="your-gemini-api-key-here"
